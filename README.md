# Github Usage bad example
**You should**
* Upload only **code and resource** to repository, upload pre-built binary via release
* Upload different project to different repo
* Do not compress your code to zip
